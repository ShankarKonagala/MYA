---
- hosts: all
  become: yes
  tasks:
  - name: Install Apache Web Server
    yum:
        name: httpd
        state: latest
    notify:
        - start apache
  handlers:
    - name: start apache
      service:
        name: httpd
        state: started

        
